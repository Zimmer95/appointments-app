/* export default interface IAppointment {
  date: String;
  time: string;
  userId: string;
  status: "active" | "canceled";
  patientName: string;
  doctorName: string;
  durationMinutes: number;
  location: string;
  notes?: string;
} */